# 2026-03-25 - 会话记录

## 会话

### 上午 (09:17 - 11:00)
- 注册 test-agents agent（`openclaw agents add`）
- 恢复丢失的 ai-baby 和 sandbox-agent 注册
- 回顾 3/16-3/24 工作历史
- 讨论记忆持久化方案
- **test-agents 记忆系统改造**：
  - 第一版：砍掉知识系统只留 markdown（过于激进，被否决）
  - 第二版：保留知识系统，搭建双向桥接
    - scripts/bridge/ — 双向同步（SQLite ↔ markdown）
    - scripts/session_recorder.py — 对话事件记录
    - scripts/unified_search.py — 统一搜索
  - 全量测试 10 项，8 通过，2 有小 bug
    - Bug 1: session_recorder --date 参数日期头错误
    - Bug 2: SQLite 重复数据（⭐前缀差异）
- 讨论 Claude Code 配置问题（MiniMax 代理过期）

### 上午续 (11:00 - 12:55)
- **继续优化 test-agents 记忆系统**：
  - 接入 Ollama 语义搜索（nomic-embed-text 768 维向量）
  - FTS5 中文分词（jieba）
  - memory_compressor.py — 日→周→月压缩
  - memory_stats.py — 全系统状态统计
  - health_check.py — 健康检查 + 自动修复
  - 智能重要性评分（关键词+类型+长度，4.5-10.0）
  - MEMORY.md 自动沉淀
  - session_recorder --sync 自动触发同步+索引
  - 配置 4 个 cron 定时任务
- **两轮全流程测试**：
  - 第一轮：test-agents 自主跑 11 步，全部通过
  - 第二轮：压力测试发现 3 个 Bug 并修复：
    1. 并发竞态 → fcntl.LOCK_EX 文件锁
    2. bridge sync 缓存失效 → 验证文件内容存在性
    3. 空文件标题恢复 → 写入时检查标题完整性
  - 最终：40 条数据一致率 100%，健康检查全绿

### 下午 (13:00 - 14:00)
- **并发安全全面加固**：
  - lock_utils.py — 统一锁工具（file_lock + md_file_lock + open_db）
  - SQLite 全部切到 WAL 模式（允许并发读+单写）
  - 3 进程并发写入验证：0 丢失
  - --sync 改为后台异步（os.fork），2.0s → 0.25s
  - 3 会话同时操作验证：5 条全在，0 hang
- **准确度优化**：
  - 重要性关键词补全（引入/切换/替代等）
  - 语义搜索分块改为细粒度（每条列表项独立，14→56 向量块）
  - nomic-embed-text task prefix（search_document/search_query）
- **切换到 bge-m3 嵌入模型**：
  - 1.2GB, 1024 维，中文效果显著提升
  - "并发安全怎么做" → "文件锁和WAL" 第 1（0.72）
  - "明天的计划" → "B3:明天需要写单元测试" 第 1（0.74）
  - 总评 8.6/10

### 下午续 (14:00 - 15:05)
- **推广到 ai-baby**：
  - 复制全套脚本，修改默认 agent 为 ai-baby
  - 修复 61 条重复数据（166→105 条）
  - 双向同步打通（最终 205 条 SQLite，一致率 99%）
  - bge-m3 语义索引 244 块
  - 配置 3 个 cron（每日索引、6h 同步、每周压缩）
- **清理 master 分支为通用模板**：
  - 删除所有 agent 特定数据（data/ai-baby/, memory/日期.md, public/知识.json）
  - MEMORY.md 等改为空模板
  - .gitignore 排除所有运行时数据
  - assume-unchanged 保护本地个人文件
  - 推送到 GitHub：master（模板）+ test-agents（实验分支）
- **workspace-setup.md 重写**：
  - 最小安装零依赖（Python 3.10+ 即可）
  - jieba（FTS5）和 Ollama（语义搜索）标为可选
  - 功能降级矩阵清晰说明

## 今日决定
- 保留知识系统，通过桥接与 OpenClaw markdown 互通（不是砍掉重建）
- 使用 bge-m3 替代 nomic-embed-text 作为语义搜索模型
- master 分支作为通用模板框架，不含 agent 特定数据

## 今日学习
- 并发安全需要 fcntl 文件锁 + SQLite WAL 模式
- nomic-embed-text 对中文短文本区分度不够，bge-m3 更适合
- 两套系统应该桥接互通，而不是砍掉其中一套
