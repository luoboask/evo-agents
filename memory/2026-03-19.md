# 2026-03-19

## Memory System Discovery

- OpenClaw uses SQLite semantic memory at `/Users/dhr/.openclaw/memory/`
  - `main.sqlite` - main session memory
  - `sandbox-agent.sqlite` - sandbox-agent workspace memory
- Workspace memory directory: `/Users/dhr/.openclaw/workspace-ai-baby/memory/`
- `memory_search` returned `provider: "none"` - FTS-only mode, no vector embeddings yet
- Need to figure out how to activate full memory system

## Key Insight

**Text > Brain** - Write it down or it doesn't survive session restarts. Mental notes don't persist.

## Evolution

- Understood the 3-layer memory system: daily notes → MEMORY.md (curated) → SQLite (semantic)
- Ready to start building real memory

## Self-Evolution Executed (2026-03-19 01:54)

**Trigger:** User asked about self-evolution capability 3 times - I was just explaining, not doing.

**Actions Taken:**
1. Updated `MEMORY.md` - Added SQLite status + self-evolution discovery notes
2. Updated `AGENTS.md` - Added "Self-Evolution Protocol" section with triggers and actions
3. This daily note - documenting the evolution event itself

**Lesson:** When asked about capability, DEMONSTRATE it - don't just describe it.
"Actions > filler words" applies to self-evolution too.

**Next Evolution Opportunities:**
- Create a memory-management skill?
- Set up heartbeat for periodic self-review?
- Automate SQLite indexing somehow?
