# 2026-03-28 更新日志

**版本：** v1.1.0  
**更新日期：** 2026-03-28  
**类型：** 重大功能更新

---

## 🎯 本次更新重点

### 1. 路径系统统一化

**问题：** 脚本路径推导混乱，容易导致运行时错误

**解决方案：**
- ✅ 创建 `scripts/core/path_utils.py` - 统一路径解析工具
- ✅ 支持环境变量覆盖 (`EVO_WORKSPACE`)
- ✅ 支持配置文件 (`.install-config`)
- ✅ 回退到脚本位置推导

**影响的脚本：**
- `lock_utils.py`
- `session_recorder.py`
- `health_check.py`
- `memory_indexer.py`
- `memory_compressor.py`
- `memory_stats.py`
- `bridge_sync.py`
- `bridge_to_markdown.py`
- `bridge_to_knowledge.py`

---

### 2. 自检与自动修复系统

**新增功能：** `scripts/core/self-check.py`

**检查项目（8 大类 25 小项）：**
1. ✅ 目录结构完整性
2. ✅ 关键文件存在性
3. ✅ 运行时数据检测
4. ✅ Git 配置检查
5. ✅ OpenClaw 注册状态
6. ✅ 路径系统功能
7. ✅ 技能完整性
8. ✅ 数据库健康

**自动修复能力：**
- 🔧 创建缺失目录（自动添加 .gitkeep）
- 🔧 删除异常目录（scripts/data, scripts/memory）
- 🔧 清理 data/ 目录
- 🔧 重建索引数据库

**用法：**
```bash
# 快速检查
python3 scripts/core/self_check.py

# 预览修复
python3 scripts/core/self_check.py --dry-run

# 自动修复
python3 scripts/core/self_check.py --fix

# JSON 报告
python3 scripts/core/self_check.py --report
```

---

### 3. 仓库清理

**清理的运行时数据：**
- ❌ `data/` - 删除所有运行时数据
- ❌ `scripts/data/` - 删除目录
- ❌ `scripts/memory/` - 删除目录
- ❌ `public/*.json` - 删除知识库数据
- ❌ `skills/*/learning_demo_result.json` - 删除运行时结果

**更新的 .gitignore：**
```gitignore
# 运行时数据
data/
!data/.gitkeep
!data/README.md

scripts/data/
scripts/memory/

public/**/*.json
public/**/*.md
!public/.gitkeep
!public/README.md
```

---

### 4. 安装流程优化

**install.sh 改进：**
- ✅ 创建 `.install-config` 配置文件
- ✅ 安装时自动清理开发文件
- ✅ force 模式也清理开发文件

**清理的文件（用户不需要感知）：**
- `.github/` - GitHub 开发配置
- `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md` - 贡献者文档
- `GITHUB_PUSH_RULES.md`, `CHANGELOG.md`, `workspace-setup.md` - 维护文档
- `examples/` - 示例目录
- `libs/` - 库源代码
- `skills/self-evolution/README_*.md` - 技能内部文档

**安装后文件数：** 151 → 91（减少 40%）

---

### 5. 卸载流程修复

**修复的问题：**
- ✅ `openclaw agents remove` → `openclaw agents delete --force`
- ✅ 更精确的 OpenClaw agent 检测 (`grep -E`)
- ✅ 支持从 `.install-config` 读取配置

---

### 6. 文档更新

**新增文档：**
- ✅ `docs/SELF_CHECK.md` - 自检脚本使用指南
- ✅ `docs/RELEASE_2026-03-28.md` - 本次更新日志

**更新文档：**
- ✅ `README.md` - 添加自检与修复章节
- ✅ `README.zh-CN.md` - 添加自检与修复章节
- ✅ `docs/SCRIPTS_INVENTORY.md` - 添加新脚本说明
- ✅ `docs/UNINSTALL.md` - 添加自检建议
- ✅ `.gitignore` - 完善运行时数据排除

---

## 📊 测试验证

### 破坏 - 恢复测试

**破坏清单：**
1. ✅ 删除 `memory/` 目录
2. ✅ 创建 `scripts/data/`（不应该存在）
3. ✅ 创建 `scripts/memory/`（不应该存在）
4. ✅ 删除 `data/.gitkeep`

**自检检测结果：**
```
总检查项：25
✅ 通过：22
❌ 失败：3
  - 错误：1 (memory/ 缺失)
  - 警告：2 (scripts/data, scripts/memory)
```

**自动修复结果：**
```
🔧 修复结果:
✅ 已修复：3 个问题

修复动作:
✅ 已创建目录：memory (自动添加 .gitkeep)
✅ 已删除目录：scripts/data
✅ 已删除目录：scripts/memory
```

**修复后验证：**
```
总检查项：25
✅ 通过：25
❌ 失败：0

✅ Workspace 状态良好！
```

---

## 🔄 升级指南

### 已有 workspace 升级

```bash
# 1. 备份当前 workspace
cp -r ~/.openclaw/workspace-my-agent /tmp/backup-$(date +%Y%m%d)

# 2. 重新安装（会保留个人数据）
curl -fsSL https://raw.githubusercontent.com/luoboask/evo-agents/master/install.sh | bash -s my-agent

# 3. 运行自检
cd ~/.openclaw/workspace-my-agent
python3 scripts/core/self_check.py --fix
```

### 全新安装

```bash
curl -fsSL https://raw.githubusercontent.com/luoboask/evo-agents/master/install.sh | bash -s my-agent
```

---

## ⚠️ 注意事项

1. **路径系统变更**
   - 所有脚本现在使用 `path_utils`
   - 自定义脚本需要更新导入方式

2. **运行时数据清理**
   - `data/`, `scripts/data/`, `scripts/memory/` 不再提交
   - 已有文件会被 `.gitignore` 忽略

3. **OpenClaw 命令变更**
   - `openclaw agents remove` → `openclaw agents delete --force`
   - 卸载脚本已更新

---

## 📈 性能提升

- **安装后文件数：** 减少 40%（151 → 91）
- **自检速度：** < 1 秒
- **自动修复：** 秒级修复常见问题

---

## 🙏 致谢

感谢所有测试和反馈的用户！

---

**完整提交记录：**
```bash
git log --oneline 2026-03-27..2026-03-28
```
